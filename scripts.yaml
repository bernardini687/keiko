env:
  DENO_ENV: dev

scripts:
  add:
    desc: Add an entry `[CATEGORY!] [AMOUNT!] [DATE]`.
    cmd: add.ts
    allow:
      - env
      - read
      - write
  budget:
    desc: Show a budget report.
    cmd: budget.ts
    allow:
      - env
      - read
  prepare:
    desc: Prepare the database.
    cmd: prepare.ts
    allow:
      - env
      - read
      - write
  review:
    desc: Review the entries.
    cmd: review.ts
    allow:
      - env
      - read
